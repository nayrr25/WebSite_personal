# Public site URL — used by metadataBase, sitemap, robots, and OG image.
# Vercel: set this in Project → Settings → Environment Variables.
NEXT_PUBLIC_SITE_URL=https://n-ai.dev

# Google Tag Manager - ID del contenedor (GTM-XXXXXXX).
# Dejalo SIN definir en local y en previews para no ensuciar las metricas con
# trafico propio. Vercel: Project -> Settings -> Environment Variables,
# marcado solo para "Production".
NEXT_PUBLIC_GTM_ID=GTM-P84F759S
